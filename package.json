{
  "name": "tythe-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "A type-safe RPC bridge between Python and TypeScript. The function signature is the contract.",
  "homepage": "https://github.com/tamimbinhakim/tythe#readme",
  "bugs": {
    "url": "https://github.com/tamimbinhakim/tythe/issues"
  },
  "license": "MIT",
  "author": "Tamim Bin Hakim (@tamimbinhakim)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tamimbinhakim/tythe.git"
  },
  "workspaces": [
    "packages/*",
    "examples/*/frontend"
  ],
  "scripts": {
    "build": "pnpm -r --filter=./packages/* run build",
    "test": "pnpm -r --filter=./packages/* run test",
    "typecheck": "pnpm -r --filter=./packages/* run typecheck",
    "lint": "pnpm lint:ts && pnpm lint:py",
    "lint:ts": "oxlint .",
    "lint:py": "uv run --directory packages/tythe ruff check .",
    "format": "pnpm format:ts && pnpm format:py && pnpm format:docs",
    "format:ts": "oxfmt --ignore-path .oxfmtignore .",
    "format:py": "uv run --directory packages/tythe ruff format .",
    "format:docs": "prettier --write \"**/*.{md,json,yaml,yml}\"",
    "format:check": "pnpm format:check:ts && pnpm format:check:py && pnpm format:check:docs",
    "format:check:ts": "oxfmt --ignore-path .oxfmtignore --check .",
    "format:check:py": "uv run --directory packages/tythe ruff format --check .",
    "format:check:docs": "prettier --check \"**/*.{md,json,yaml,yml}\"",
    "clean": "pnpm -r run clean && rimraf node_modules .turbo",
    "prepare": "husky",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "21.0.0",
    "@commitlint/config-conventional": "21.0.0",
    "husky": "9.1.7",
    "lint-staged": "17.0.4",
    "oxfmt": "0.49.0",
    "oxlint": "1.64.0",
    "prettier": "3.8.3",
    "rimraf": "6.1.3",
    "typescript": "6.0.3"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,mjs,cjs}": [
      "oxlint --fix",
      "oxfmt --ignore-path .oxfmtignore"
    ],
    "packages/tythe/**/*.py": [
      "uv run --directory packages/tythe ruff check --fix",
      "uv run --directory packages/tythe ruff format"
    ],
    "*.{md,json,yaml,yml}": [
      "prettier --write"
    ]
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@11.0.9"
}
