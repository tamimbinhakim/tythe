# Copy to `.env` for local dev. Never commit `.env`.

# Tythe dev server
TYTHE_HOST=127.0.0.1
TYTHE_PORT=8000

# Where to write the generated TS client (relative to repo root)
TYTHE_CLIENT_OUT=examples/vite-react/frontend/src/lib/tythe/client.ts
